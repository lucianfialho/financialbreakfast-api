# Railway PostgreSQL Database URL
# Fornecida automaticamente pelo Railway quando você adiciona o serviço PostgreSQL
DATABASE_URL=postgresql://user:password@host:port/database

# Port (Railway fornece automaticamente)
PORT=8000

# Ambiente
ENVIRONMENT=production